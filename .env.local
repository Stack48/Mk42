# Utilisé par docker-compose (la DATABASE_URL est écrasée par le service app)
DATABASE_URL=postgresql://commissionpro:commissionpro_password@localhost:5433/commissionpro

# Taux de commission par défaut (en %)
DEFAULT_COMMISSION_RATE=5
